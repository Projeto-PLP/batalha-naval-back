meta {
  name: Criar Partida IA
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/match
  body: json
  auth: inherit
}

body:json {
  {
    "mode": "Classic",
    "aiDifficulty": "Basic"
  }
}

script:post-response {
  const data = res.getBody();
  bru.setEnvVar("matchId", data.matchId);
}

settings {
  encodeUrl: true
  timeout: 0
}
